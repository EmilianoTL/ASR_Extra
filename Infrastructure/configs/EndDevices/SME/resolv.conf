# /etc/resolv.conf — SME
# DNS público de Google (sale por eth1/NAT gracias a la ruta /32 de sme-routes.sh).
nameserver 8.8.8.8
# DNS secundario opcional (descomenta si lo necesitas):
# nameserver 1.1.1.1
