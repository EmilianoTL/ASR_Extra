# /etc/resolv.conf — SME
# DNS público Cloudflare (sale por eth1/NAT siguiendo la ruta por defecto).
# Se eligió 1.1.1.1 para evitar el solapamiento con la red de topología 8.8.8.0/24.
nameserver 1.1.1.1
# DNS secundario opcional (descomenta si lo necesitas):
# nameserver 1.0.0.1
