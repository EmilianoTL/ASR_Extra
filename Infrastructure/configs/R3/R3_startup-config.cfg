! =====================================================================
!  R3 — Cisco c7200 (IOS)   |  Config base para el monitoreo ASR
! ---------------------------------------------------------------------
!  Topología en CADENA: R1 -- R2 -- R3 (R3 solo se conecta a R2).
!  Interfaces:
!    f0/0 = LAN PC2  148.204.60.1/24
!    f1/1 = enlace P2P hacia R2  8.8.8.6/30  (8.8.8.4/30)
!  NO se configura ningún protocolo de enrutamiento.
!  'crypto key generate rsa general-keys modulus 1024' se ejecuta manualmente.
! =====================================================================
hostname R3
!
ip domain-name lab.local
!
enable secret enable123
username admin privilege 15 secret cisco123
!
! --- LAN de R3 (PC2) ---
interface FastEthernet0/0
 description LAN R3 (PC2) 148.204.60.0/24
 ip address 148.204.60.1 255.255.255.0
 no shutdown
!
! --- Enlace inter-router (/30) hacia R2 ---
interface FastEthernet1/1
 description Enlace P2P hacia R2 (8.8.8.4/30)
 ip address 8.8.8.6 255.255.255.252
 no shutdown
!
ip ssh version 2
line vty 0 4
 transport input ssh
 login local
!
snmp-server group GRUPO_V3 v3 priv
snmp-server user snmp_user GRUPO_V3 v3 auth sha authpass123 priv des56 privpass123
snmp-server enable traps
snmp-server host 148.204.56.10 version 3 priv snmp_user
snmp-server location Lab-ASR-Site-R3
snmp-server contact admin@lab.local
!
cdp run
!
! --- SIN protocolo de enrutamiento (lo activa la app) ---
!
end
