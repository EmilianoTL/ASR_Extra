! =====================================================================
!  R2 — Cisco c7200 (IOS)   |  Config base para el monitoreo ASR
! ---------------------------------------------------------------------
!  AJUSTA las IPs a tu topología real antes de aplicar.
!    - Core de gestión:   8.8.8.0/24  (R2=.5) vía Sw1
!    - LAN de R2:         148.204.59.0/24  (gateway .1)
!  NO se configura ningún protocolo de enrutamiento.
!  'crypto key generate rsa' se ejecuta manualmente (ver README).
! =====================================================================
hostname R2
!
ip domain-name lab.local
!
enable secret enable123
username admin privilege 15 secret cisco123
!
interface FastEthernet0/0
 description Core de gestion hacia Sw1 (8.8.8.0/24)
 ip address 8.8.8.5 255.255.255.0
 no shutdown
!
interface FastEthernet0/1
 description LAN R2 (148.204.59.0/24)
 ip address 148.204.59.1 255.255.255.0
 no shutdown
!
ip ssh version 2
line vty 0 4
 transport input ssh
 login local
!
snmp-server group GRUPO_V3 v3 priv
snmp-server user snmp_user GRUPO_V3 v3 auth sha authpass123 priv aes 128 privpass123
snmp-server enable traps
snmp-server host 8.8.8.100 version 3 priv snmp_user
snmp-server location Lab-ASR-Site-R2
snmp-server contact admin@lab.local
!
cdp run
!
! --- SIN protocolo de enrutamiento (lo activa la app) ---
!
end
